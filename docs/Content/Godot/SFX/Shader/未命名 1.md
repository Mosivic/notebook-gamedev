``` js
shader_type canvas_item

//定义变量
//uniform 全局变量，可从编辑器中输入 
uniform sampler2D picture;

//varying 传递变量，用于从顶点处理器往片段/灯光处理器函数中传递数据
varying float some = 0.0;

//普通变量，在函数外只能定义为const常量
const int value_t = 6;

void fragment(){
	COLOR = ver4(1.0,0.0,0.0,1.0);//对片元颜色进行赋值
	COLOR = texture(TEXTURE,UV); //读取节点纹理颜色赋值给片元

	vec2 uv = UV；
	uv = uv + 0.5; //改变uv的映射关系，uv改变为xy（0.5->1.5）,即只显示原uv的xy（0.5->1.0）部分，可视为加是图像左移，减是图像右移

	/*uv绕中心旋转
	vec2 uv_ = UV;
	uv_ = uv_ - 0.5;//将uv移动到中点
	mat rotate_m = mat2(vec2(cos(angle),-sin(angle)),vec2(sin(angle),cos(angle));
	uv_ = uv_ * m;//uv进行旋转
	uv_ = uv_ 0.5;//将uv移回到图像位置
	*/
	
	//uv的翻转
	uv.y = 1.0 - uv.y;//上下翻转
	uv.x = 1.0 - uv.x;//左右翻转
  }
```

#### 播放序列帧图
``` js
shader_type canvas_item;

vec2 flipbook(vec2 uv, vec2 size, float progress){
	progress = floor(mod(progress, (size.x * size.y)));
	vec2 frame_size = vec2(1.0, 1.0) / vec2(size.x, size.y);

	float h = mod(progress, size.x);
	float v = floor(progress / size.x);
	
	vec2 hv = vec2(h, v);
	vec2 frame = (uv / size) + hv * frame_size;
	
	return frame;
}

void fragment()
{
	vec2 flip_uv = flipbook(UV, vec2(4.0, 4.0), TIME * 10.);
	vec4 flip_texture = texture(TEXTURE, flip_uv);
	COLOR = flip_texture;
}
```